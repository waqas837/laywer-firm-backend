'use strict';

/**
 * admin-socket-record router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::admin-socket-record.admin-socket-record');
